navigation
.container
  .row
    .col-md-6
      h1 Register
      p.lead
        | Already a member? Please 
        a(href='login') log in 
        |  instead.
      form(ng-submit='vm.onSubmit()')
        .form-group
          label(for='username') Username 
          input#username.form-control(type='text', placeholder='Enter your username', ng-model='vm.credentials.username')
        .form-group
          label(for='password') Password 
          input#password.form-control(type='password', placeholder='Password', ng-model='vm.credentials.password')
        button.btn.btn-default(type='submit' id='register_button') Register!
