navigation
.container
  .row
    .col-md-6
      h1 Register
      p.lead
        | Already a member? Please 
        a(href='login') log in 
        |  instead.
      form(ng-submit='vm.onSubmit()')
        formly-form(model='vm.credentials', fields='vm.credentialsFields' form='vm.credentialsForm')
          button.btn.btn-default(type='submit' id='register_button') Register!
