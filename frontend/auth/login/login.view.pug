navigation
.container
  .row
    .col-md-6
      h1.form-signin-heading Sign in
      p.lead
        | Not a member? Please 
        a(href='register') register
        |  instead.
      form(ng-submit='vm.onSubmit()')
        .form-group
          label(for='username') Username 
          input#username.form-control(placeholder='Enter username', ng-model='vm.credentials.username')
        .form-group
          label(for='password') Password 
          input#password.form-control(type='password', placeholder='Password', ng-model='vm.credentials.password')
        button.btn.btn-default(type='submit' id='login_button') Sign in!
